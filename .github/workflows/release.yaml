name: Release - Module

on:
  workflow_dispatch:
    inputs:
      release-version:
        description: Release Version
        required: true
        default: 1.0.0

run-name: 'version set to ${{ inputs.release-version }} for release'

jobs:
  main:
    uses: codbex/codbex-infra/.github/workflows/module-release.yaml@main
    with:
      packages: codbex-accounts-data
      packages-build: codbex-accounts-data
      release-version: ${{ inputs.release-version }}
      release-content: |
        ## codbex-accounts-data - ${{ inputs.release-version }}
  
        Accounts Data Module
  
        ## Installation
  
        ```
        npm install @codbex/codbex-accounts-data@${{ inputs.release-version }}
        ```
  
    secrets: inherit
